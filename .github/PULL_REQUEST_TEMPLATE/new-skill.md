## Summary

<!-- What new rewrite rule / skill does this add, and why? -->

## Example

<!-- Before/after example showing the new rule in action -->

```
Before:
After:
```

## Checklist

- [ ] Rule is conservative and doesn't change the meaning of the text
- [ ] Any randomness goes through the seeded `random.Random` instance
- [ ] Replacement preserves capitalization (see `_match_case`)
- [ ] Tests added/updated in `tests/test_core.py`
- [ ] `README.md` updated if public API/CLI changed
- [ ] `pytest` passes locally
